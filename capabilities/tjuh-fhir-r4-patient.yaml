naftiko: 1.0.0-alpha2
info:
  label: Thomas Jefferson University Hospital FHIR R4 — Patient
  description: Read and search Patient demographics on Jefferson Health's Epic FHIR R4 endpoint under SMART on FHIR scopes.
  tags:
  - Jefferson Health
  - Epic
  - FHIR
  - Patient
  - USCDI
  created: '2026-05-23'
  modified: '2026-05-23'
binds:
- namespace: env
  keys:
    JEFFERSON_FHIR_TOKEN: JEFFERSON_FHIR_TOKEN
capability:
  consumes:
  - type: http
    namespace: tjuh-fhir-r4-patient
    baseUri: https://fhir.jefferson.edu/FHIRProxy/api/FHIR/R4
    description: Patient resource on the Thomas Jefferson University Hospital FHIR R4 endpoint.
    resources:
    - name: Patient
      path: /Patient
      operations:
      - name: searchPatient
        method: GET
        description: Search Patient by identifier, name, birthdate, or gender.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: identifier
          in: query
          type: string
          description: Patient identifier (e.g. MRN, system|value).
        - name: name
          in: query
          type: string
          description: Patient name search.
        - name: birthdate
          in: query
          type: string
          description: Patient date of birth.
      - name: readPatient
        method: GET
        path: /Patient/{id}
        description: Read Patient by id.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: Patient logical id.
    authentication:
      type: bearer
      token: '{{env.JEFFERSON_FHIR_TOKEN}}'
  exposes:
  - type: rest
    namespace: tjuh-fhir-r4-patient-rest
    port: 8080
    description: REST adapter for TJUH FHIR R4 Patient.
    resources:
    - path: /v1/patient
      name: patient
      description: REST surface for Patient.
      operations:
      - method: GET
        name: searchPatient
        description: Search Patient.
        call: tjuh-fhir-r4-patient.searchPatient
        with:
          identifier: rest.identifier
          name: rest.name
          birthdate: rest.birthdate
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: tjuh-fhir-r4-patient-mcp
    port: 9090
    transport: http
    description: MCP adapter for TJUH FHIR R4 Patient.
    tools:
    - name: search-patient
      description: Search Patient on Jefferson Health FHIR R4.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: tjuh-fhir-r4-patient.searchPatient
      with:
        identifier: tool.identifier
        name: tool.name
        birthdate: tool.birthdate
    - name: read-patient
      description: Read Patient by id on Jefferson Health FHIR R4.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: tjuh-fhir-r4-patient.readPatient
      with:
        id: tool.id
